name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # Trigger the workflow on pushes to the main branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest # Use the latest Ubuntu runner

    steps:
      # Step 1: Checkout the repository code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: List files in the root directory to confirm 'frontend' exists
      - name: List files after checkout
        run: |
          ls -al

      # Step 3: Set up Node.js environment
      - name: Set up Node.js
        uses: actions/setup-node@v4.1.0
        with:
          node-version: "latest"

      # Step 4: Install frontend dependencies
      - name: Install frontend dependencies
        run: |
          cd frontend
          npm install

      # Step 5: Build the project with Vite
      - name: Build project
        run: |
          cd frontend
          npm run build
          # List the contents of the 'frontend' and 'frontend/dist' directories after build
          ls -al frontend
          ls -al frontend/dist

      # Step 6: Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: frontend/dist # Specify the build output folder (should match your Vite config)

    permissions:
      contents: write # Ensure the GitHub token has write access to contents
